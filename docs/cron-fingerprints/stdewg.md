# Cron `stdewg` — velib-mcp slice

- job-id: `stdewg`
- oldest-issue-id: `one_track#2`
- parent-fingerprint: `8t5mom` (PR #208)
- tick-date: 2026-06-24
- scope: oldest 15 author:DominicBurkart open issues across scoped repos
- slice: velib-mcp (1 issue: #9)
- sibling slices: nanna-coder (#5 #10 #20 #23 #24 #39), one_track (#2–#9)

## Fingerprint result — byte-identical no-op vs `8t5mom`

| target            | head SHA @ `8t5mom` | head SHA @ `stdewg` | Δ |
|-------------------|---------------------|---------------------|---|
| `main` (base)     | `579ddb0b`          | `579ddb0b`          | — |
| #142 (canonical)  | `bbaaeb64`          | `bbaaeb64`          | — |
| #114 (parallel)   | `83ff42ed`          | `83ff42ed`          | — |

`main` has not advanced in this slice for 30+ days. #142's head has not moved since 2026-04-29.

## Per-issue disposition

| issue | canonical PR | head sha | action |
|------:|-------------:|----------|--------|
| #9    | #142         | `bbaaeb64` | NO-OP — defer (`mergeable_state=clean`, idle 34 days) |

Parallel PR #114 (`describe_api`, `dirty`) covers the same issue with a narrower shape. Janitor dedup applies — recommend keeping #142.

## Why this CRON ships no code

1. Canonical PR #142 already implements `docs/describe` + `velib://docs/api` end-to-end and is `mergeable_state=clean`.
2. Spawning a parallel implementation creates duplicate-PR sprawl (CRON rule 2).
3. The bottleneck is **promotion**, not implementation. #142 has been clean and idle for over a month waiting for a janitor/owner promote decision.

## Janitor carry-over

- **Promote #142 to `ready-for-review` and merge it.** Highest-value janitor action across all 3 slices of `stdewg`. #142 has been `clean` for 34 days.
- Close #114 as duplicate.
- Close superseded `claude/determined-brown-*` fingerprint PRs on sight — keep `stdewg` (this PR), close `8t5mom` (#208), and ALL earlier siblings (#207, #206, #205, #204, #203, #202, #201, #200, #199, #198, #197, #196, #195, #194, #193, #192, #191, #190, #189, #188, #187, #186, #185, #184, #183, #182, #181, #180, #177, #168, #165). Keep #197 — it is feature content (`feat(kani): foundation lints`), not a fingerprint.

## Promote-to-human

Per CRON contract clause (3) — **planner jobs never promote**. This PR is intentionally **NOT** labeled `ready-for-review`. Janitor decides.

## Tags

- `agent-job:stdewg`
- `oldest:one_track-2`

---

_Generated by stateless CRON `stdewg` (one-shot, LIFO issue-owner routine — claude-opus-4-7)._
